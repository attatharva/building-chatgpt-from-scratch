Midterm Submission


Summer of Code

Name:Atharva Tiwari  


Project:Building ChatGPT from Scratch

The following is the list of things I have learned up to now:


## Necessary DS Tools

### Python
Variables, Strings, Arithmetic operators, Logical operators, if statements, while loops, Lists (append, insert, remove, clear), for loops, range function, Tuples (unchangeable unlike lists).

### NumPy
NumPy vs Lists (speed, functionality), Applications of NumPy, The Basics (ndim, shape, dtype, itemsize, nbytes, size), Accessing/Changing Specific Elements, Rows, Columns, Initializing Different Arrays (1s, 0s, full, random, etc...), Basic Mathematics (arithmetic, trigonometry, etc.), Linear Algebra, Statistics, Reorganizing Arrays (reshape, vstack, hstack), Load data in from a file, Advanced Indexing and Boolean Masking.

### Pandas
Loading data into Pandas, Reading Data (Getting Rows, columns, Cells, Headers), Iterate through each Row, Getting rows based on a specific condition, High Level description of your data (min, max, mean, std dev), Sorting Values (Alphabetically, Numerically), Making Changes to the DataFrame (Adding a column, Deleting a column, Summing Multiple Columns to Create new Column), Rearranging columns, Saving Data (CSV, Excel, TXT), Filtering Data (based on multiple conditions), Reset Index, Regex Filtering (filter based on textual patterns), Conditional Changes, Aggregate Statistics using Groupby (Sum, Mean, Counting), Working with large amounts of data.

### Matplotlib
Making a line graph, Add title and labels for x & y axis, Change font type, size, etc., Change tick marks (scale graph), Add a legend, Restyle our line (color, line style, markers, width), Shorthand notation to restyle lines, Line customization options, Plotting more complex lines, Resizing Graph, Saving Graph, Annotating/Cleaning Code, Bar Chart.



## Machine Learning

### Supervised Learning
Linear regression model, Polynomial regression, Logistic regression, Decision boundary, Overfitting and underfitting, Regularization, Cost function, Learning rate, Vectorization, Feature scaling, Support Vector Machines (radial kernel), Neural networks (TensorFlow), Neural network training, Activation functions, Multiclass classifications, Bias and variance, Backpropagation, Gradient descent, ReLU activation function, ArgMax and SoftMax, Cross entropy, Decision trees (tree ensembles).

### Deep Learning & NLP
Image classification with CNN's, Recurrent Neural Networks (RNN's), Long Short-Term Memory (LSTM), Word embedding, Seq2Seq, Encoder-Decoder neural networks, Attention for neural networks.
